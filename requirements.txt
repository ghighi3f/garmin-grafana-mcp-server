# Core MCP + web stack
mcp>=1.2.0
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-dotenv>=1.0.0

# InfluxDB v1 (default for garmin-grafana)
influxdb>=5.3.2

# InfluxDB v2 (optional — install if INFLUXDB_VERSION=2)
# influxdb-client>=1.42.0
